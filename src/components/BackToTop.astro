<a href="#top" id="back-to-top" class="back-to-top" aria-label="Terug naar boven">↑</a>

<style>
  .back-to-top {
    position: fixed;
    right: 22px;
    bottom: 22px;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--ink);
    color: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    z-index: 40;
    font-size: 18px;
    line-height: 1;
  }

  .back-to-top.show {
    opacity: 1;
    pointer-events: auto;
  }
</style>
